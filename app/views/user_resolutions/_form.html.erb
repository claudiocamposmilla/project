<%= form_with(model: user_resolution) do |form| %>
  <% if user_resolution.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_resolution.errors.count, "error") %> prohibited this user_resolution from being saved:</h2>

      <ul>
        <% user_resolution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :resolution_id, style: "display: block" %>
    <%= form.text_field :resolution_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
